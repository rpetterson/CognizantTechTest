package com.qaselenium.tests;

import com.qaselenium.framework.utils.testCase.AbstractTestCase;


public class ApiTest extends AbstractTestCase {

/*	public Reporter REPORTER = AbstractTestCase.REPORTER;
	public XmlUtil xmlutil = new XMLUtility();

	Response httpResponse;

	@Test
	public void getConfiguration() {

		WebClientUtil webClient = new WebClientUtil();

		Map<String, String> headers = new LinkedHashMap<String, String>();

		headers.put("Accept", "text/html");
		REPORTER.Info("Test First Ford API");httpResponse = webClient.makeCall("GET", "https://ford/fordapi",headers, null, null);

		verifyResponseHeader(httpResponse.getStatusCode(), "200");

	}

	public void verifyResponseHeader(String actualResponse,
			String expectedResponse) {
		// compare actual and expected results and report the outcome
		if (actualResponse.toString().equals(expectedResponse)) {
			REPORTER.Pass("Verify Response Header",expectedResponse.toString(), actualResponse.toString(),httpResponse.getBody());
		} else {
			REPORTER.Fail("Verify Response Header",expectedResponse.toString(), actualResponse.toString(),httpResponse.getBody());

		}

	}*/


}
